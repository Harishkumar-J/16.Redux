1. npm install
2. npm install redux
redux is a library which can also work in normal javascript and so to connect with react -> install

3.npm install react-redux


4.npm install @reduxjs/toolkit
//we can unistall redux now